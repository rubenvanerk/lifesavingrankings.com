<div class="mt-1">
    <input type="{{ $type }}" name="{{ $name }}" id="{{ $id }}"
           {{ $attributes->merge(['class' => 'shadow-sm focus:ring-yellow-400 focus:border-ring-400 block w-full sm:text-sm border-gray-300 rounded-md text-gray-900']) }}>
</div>
